{
  "name": "mytoro",
  "version": "1.0.0",
  "description": "MyToro trading game with live data and local history",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "npx playwright test",
    "test:headed": "npx playwright test --headed",
    "test:report": "npx playwright show-report",
    "test:mock": "npx playwright test tests/regression.spec.js --grep 'Live trading regression suite'",
    "test:live": "npx playwright test tests/regression.spec.js --grep 'Live trading regression suite'"
  },
  "dependencies": {
    "express": "^4.19.2"
  },
  "devDependencies": {
    "@playwright/test": "^1.39.1"
  }
}
